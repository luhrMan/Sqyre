[package]
name = "sqyre-ports"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "OS-facing port traits (capture, automation, focus) and execution telemetry shared by adapters and executor"

[lints]
workspace = true

[dependencies]
image = { workspace = true }
parking_lot = { workspace = true }
rayon = { workspace = true }
sqyre-domain = { path = "../sqyre-domain" }
thiserror = { workspace = true }
